{
    "name": "figma",
    "description": "A web hosted version of figma plugin.",
    "authors": "Grida.co",
    "version": "0.0.0",
    "private": false,
    "dependencies": {
        "@ui/skeleton": "0.0.0"
    },
    "scripts": {
        "build": "webpack --mode=production --host=production",
        "build:staging": "webpack --mode=development --host=staging",
        "build:dev": "webpack --mode=development --host=dev",
        "watch": "webpack --watch",
        "dev": "yarn watch"
    }
}
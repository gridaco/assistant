# Slot definition and management.


*default dev-mode slot convention*
- name should start with `"slot:"` in form of `"slot:slot-name"`
- default name based supported variables are ..
  - text
    - text: string
  - image (on shape)
    - src: string
  - any
    - visible: boolean
import React from "react";
import styled from "@emotion/styled";

export function PhotoLoader() {
  return <Wrapper>photo Loader</Wrapper>;
}
const Wrapper = styled.div`
  width: 100%;
`;

const Inner = styled.div`
  margin: 16px;
  width: 100%;
`;

const ImageLoader = styled.div``;
